# States are basically the data in general
# Here data transfer objects(DTOs) are used to specify the format in which the data has to be transmitted


from . import SessionDTO, BookDTO, UserDTO, EmbeddedDocumentDTO
